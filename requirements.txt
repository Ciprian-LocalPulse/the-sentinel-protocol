cryptography>=42.0
jsonschema>=4.21
pytest>=8.0
pytest-cov>=4.1

# API server (prototype/python/api/server.py, docs/api/)
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27  # required by fastapi.testclient for tests/test_api.py

# Optional, for production-grade NLP (docs/persona/stylometry.md mentions
# spaCy for full POS tagging; the reference implementation ships a
# dependency-free approximation instead so the prototype installs cleanly).
# spacy>=3.7
